# logging ?? ??
logging:
  level:
    root: INFO
    sql: ERROR
# Overloaded POST ??
spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
# session
server:
  servlet:
    session:
      timeout: 10m
